data:
  sources: []
  output_dir: data/processed
  tokenizer_dir: artifacts/tokenizer
  max_length: 2048
  validation_ratio: 0.02
  seed: 42
  min_text_chars: 2
  languages: [en, yo, ig, ha, fr, es, de, pt, zh, ja, ko, ar]
  shard_size: 10000
  balance_by: language
